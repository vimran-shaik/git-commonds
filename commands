Following are the frequently using git commands,
* git --version
* git init
* git add
* git status
* git commit -m "message"
* git commit -a -m "message"
* git config -global
* git remote add origin <github account link>
* git push -u origin master
* git clone <link of the github>
* git remote -v
* git pull <github link> / git pull origin master
* git logs
* git branch <branch name>
* git branch --lis
* git checkout <branch name>
* git merge <branch name>
* git reset --merge
* vi .gitignore (to restrict any file to track)

